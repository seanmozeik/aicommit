/** Directory name under `~/.config` (override with `AIC_CONFIG_HOME`). */
export const CONFIG_DIR_NAME = 'aicommit';

/** OS keychain item storing the JSON secrets blob (`Bun.secrets`). */
export const KEYCHAIN_SERVICE = 'aicommit';
export const KEYCHAIN_ACCOUNT = 'aicommit';
